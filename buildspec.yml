version: 0.2

phases:
  pre_build:
    commands:
      - echo 'TESTING NOW'
      - mvn --version
      - cd ./Customer
      - ls
      - mvn spring-boot:build-image -DskipTests
      - docker image ls
  
  build:
    commands:
      - echo 'TEST'